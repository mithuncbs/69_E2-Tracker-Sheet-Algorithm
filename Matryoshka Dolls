#include <bits/stdc++.h>
using namespace std;

int main()
{
    long long s, x;
    if (cin >> s >> x)
    {
        int count = 0;

        while (s > 0)
        {
            count++; 
            s /= x;  
        }
        
        cout << count << "\n";
    }

    return 0;
}

#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;

    while (t--)
    {
        int n;
        cin >> n;

        int arr[n];
        for (int i = 0; i < n; i++)
            cin >> arr[i];

        int count = 0;

        for (int i = 1; i < n; i++)
        {
            for (int j = 1; j <= n - i; j++)
            {
                if (arr[j - 1] > arr[j])
                {
                    swap(arr[j - 1], arr[j]);
                    count++;
                }
            }
        }

        cout << "Optimal train swapping takes " << count << " swaps." << endl;
    }

    return 0;
}

#include <iostream>
using namespace std;

int main()
{
    int t;
    cin >> t;

    for (int i = 1; i <= t; i++)
    {
        int a, b, c;
        cin >> a >> b >> c;

        int p, q;
        cin >> p >> q;

        int total = 0;

        if (q >= p && c > 0 && a > 1)
        {
            for (int i = 1; i <= c; i++)
            {
                if (a > 1 && i > 0)
                {
                    a = a - 2;
                    total = total + q;
                }
                else
                    break;
            }

            if (a > 1 && b > 0)
            {
                for (int i = 1; i <= b; i++)
                {
                    if (a > 1 && i > 0)
                    {
                        a = a - 2;
                        total = total + p;
                    }
                    else
                        break;
                }
            }
        }

        else
        {
            for (int i = 1; i <= b; i++)
            {
                if (a > 1 && i > 0)
                {
                    a = a - 2;
                    total = total + p;
                }
                else
                    break;
            }

            if (a > 1 && c > 0)
            {
                for (int i = 1; i <= c; i++)
                {
                    if (a > 1 && i > 0)
                    {
                        a = a - 2;
                        total = total + q;
                    }
                    else
                        break;
                }
            }
        }

        cout << total << endl;
    }

    return 0;
}

#include <iostream>
#include <string>
using namespace std;

int main()
{
    int n;
    cin >> n;

    string s;
    cin >> s;

    string st;
    int skip = 1;

    for (int i = 0; i < n; i += skip)
    {
        st.push_back(s[i]);
        skip++;
    }

    cout << st << endl;

    return 0;
}

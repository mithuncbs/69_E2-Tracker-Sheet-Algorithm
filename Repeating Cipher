#include <iostream>
#include <string>
using namespace std;

int main()
{
    int n;
    cin >> n;

    string t;
    cin >> t;

    string s = "";
    int skip = 1;

    for (int i = 0; i < n; i += skip)
    {
        s += t[i];
        skip++;
    }

    cout << s << endl;

    return 0;
}
